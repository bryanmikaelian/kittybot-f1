// Core represents the foundation of Kittybot.  All generic message that are in the room are handled here.

this.respond = function(client, message, callback) {
  // Figure out what the command is and generate a response
}
